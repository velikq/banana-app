rewrite code to work primarily with PNG files (all model requests require PNG output; all new generations are saved as valid PNG files) with option to load JPEG files when querying generation (load jpegs as references)
